"""Main application module.

This module defines an instance of the facade, and an instance of a subclass of app.core.application.FastAPIFactory, called `appfactory`,
that takes the facade instance into its constructor."""
